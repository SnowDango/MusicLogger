<resources>

    <!-- Base application theme. -->
    <style name="Theme.MusicLogger" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="background">@color/backGround</item>
        <item name="cardBackgroundColor">@color/backGround</item>
        <item name="textFillColor">@color/textColor</item>
    </style>

    <style name="Theme.MusicLogger.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>